/**
 * Returns the length of the input array.
 *
 * @example
 * // returns 7
 * len( list( range( 7 ) ) ) ;
 *
 * @param {Array} array - The input array.
 * @returns {Number}
 *
 */
export function len ( array ) {

	return array.length ;

}
